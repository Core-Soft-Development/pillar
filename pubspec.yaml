name: pillar_workspace
description: A Flutter monorepo for building scalable applications
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

# Workspace is managed by melos.yaml

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  melos: ^6.0.0

flutter:
  uses-material-design: true
