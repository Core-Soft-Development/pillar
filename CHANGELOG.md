# Changelog

Workspace-level changelog. Entries below this point are generated by
`melos version` from Conventional Commits — see
[docs/VERSIONING.md](docs/VERSIONING.md).

Each package also keeps its own `CHANGELOG.md`, which is what its GitHub
release notes are built from. That is the one to read if you consume a single
package.

Nothing has been published to pub.dev yet.
