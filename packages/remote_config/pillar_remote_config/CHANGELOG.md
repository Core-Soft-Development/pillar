# Changelog

All notable changes to this package are documented in this file.

This package follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
Entries below are generated by `melos version` from conventional commits.

## 1.0.0

### Added

- Initial release of `pillar_remote_config`.
- `RemoteConfigService` — typed access to remote parameters with defaults.
- `RemoteConfigRepository` — fetch, activate and cache remote values.
- `RemoteConfigProvider` — Flutter integration on top of `pillar_core`'s
  dependency injection.
