name: pillar_core_example
description: Console example for pillar_core — modules, lifetimes and scopes.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  pillar_core: ^1.0.0

dev_dependencies:
  lints: ^5.0.0
