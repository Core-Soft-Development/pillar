# An example is read, not published: it prints, and it documents itself in prose
# rather than in dartdoc.
include: ../analysis_options.yaml

linter:
  rules:
    avoid_print: false
    public_member_api_docs: false
