# Flutter packages. Pure-Dart packages carry their own analysis_options.yaml
# that includes package:lints instead — see packages/pillar_core.
include:
  - package:flutter_lints/flutter.yaml
  - analysis_options_base.yaml

linter:
  rules:
    # Flutter-only rules, on top of the shared set.
    - avoid_unnecessary_containers
    - avoid_web_libraries_in_flutter
    - diagnostic_describe_all_properties
    - no_logic_in_create_state
    - prefer_const_literals_to_create_immutables
    - sized_box_for_whitespace
    - sort_child_properties_last
    - use_build_context_synchronously
    - use_full_hex_values_for_flutter_colors
    - use_key_in_widget_constructors
